node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust / Tauri build artifacts
src-tauri/target/
src-tauri/gen/

# Tauri release script — holds the local signing key, never committed
tools/build-release.ps1

# local design mocks + renders (kept on disk, not tracked)
design-preview/

# downloaded SRD source (re-fetchable; converters in tools/srd/ read it → the CONTENT repo's CSVs)
tools/srd-src/

# generated: content vendored into static/ for the build (source of truth = the charnik-content-srd repo)
static/content/

# local: where THIS machine keeps the content repo, when it isn't the sibling default
charnik.dev.json
charnik.config.json

# CI: where the workflows check the content repo out (a sibling path is outside the workspace)
.content-srd/

# vitest browser-mode failure screenshots + test attachments
__screenshots__/
.vitest-attachments/

# test coverage output
coverage/

# editor
.vscode/
.idea/

# visual-regression screenshots (local, machine/font-specific)
tools/visual/baseline/
tools/visual/current/


# dated QA reports are working checklists, never history
docs/report-*.md
gource.mp4
